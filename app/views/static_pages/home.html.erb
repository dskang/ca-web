<body class="cover">
  <%= render "nav" %>
  <div class="wrapper">
    <div class="container">
      <% if user_signed_in? %>
        <%= render "schools/share" %>
      <% else %>
        <%= render "how_it_works" %>
      <% end %>
      <%= render "schools" %>
      <%= render "footer" %>
    </div>
  </div>
</body>
